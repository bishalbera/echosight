# EchoSight Frontend — Environment Variables
# cp .env.local.example .env.local

# ── Stream app credentials ──
# Get from: dashboard.getstream.io → your app

# Public — safe to expose in the browser
NEXT_PUBLIC_STREAM_API_KEY=your_stream_api_key

# Server-side only — used by /api/token to sign JWTs, never sent to the browser
STREAM_API_SECRET=your_stream_api_secret

# The user ID for the demo user (can be anything, used as JWT subject)
NEXT_PUBLIC_STREAM_USER_ID=demo-user

# Call ID to join (optional — defaults to "echosight-demo")
# NEXT_PUBLIC_STREAM_CALL_ID=echosight-demo
